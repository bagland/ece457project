function [best] = adaptiveGA()

for i=1:10
 results, numIt = GA();
 best(i) = results(1);
 numIt(i) = results(2);
end

figure;
histfit(best);
disp(mean(best));
disp(min(best));

figure(2);
histfit(numIt);
disp(mean(numIt));
disp(min(numIt));